# Dependencies
node_modules
.npm

# Build outputs
.next
dist
build
out
.turbo
*.tsbuildinfo

# Uploaded documents (local disk storage — bind-mounted into the API container)
/storage/
.storage/

# Environment files — never commit secrets
.env
.env.development.local
.env.test.local
.env.production.local
.env.*.local
apps/api/.env.local
apps/web/.env.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime
*.pid
*.seed
*.pid.lock

# Coverage
coverage
*.lcov
.nyc_output

# IDE
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Prisma — migration SQL is the source of truth for `prisma migrate deploy`
# and MUST be committed, or deploys apply zero migrations and come up with an
# empty (table-less) database.

# Sentry
.sentryclirc

# Local-only diagnostic / one-off scripts (run manually on the VPS, never committed)
apps/api/prisma/diagnose-client.ts

# Claude
.claude
.turbo
/planning

# Graphify
graphify-out
config.bat
